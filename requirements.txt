# GD Skill Match — Python dependencies.
# The web server itself uses only the Python standard library; numpy/scipy/sklearn
# are needed by the data pipeline + recommendation engine. google-cloud-storage and
# functions-framework are only used when deployed to GCP (Cloud Run + Cloud Function).
numpy>=1.26
scipy>=1.10
scikit-learn>=1.3
google-cloud-storage>=2.10
functions-framework>=3.5
